    class Program
    {
        static void Main(string[] args)
        {
            const int sample1 = 0x3A;

            byte sample2 = 58;
            int heartRate = 85;
            double deposits = 135002796;
            const float acceleration = 9.800;
            var float mass = 14.6;
            double distance = 129.763001;
            force = mass * acceleration;
            bool lost = true;
            bool expensive = true;
            int choice = 2;
            char integral = "\u222";
            const string = "Hello";
            string name = "Karen";

            if (sample1 == sample2)
            {
                Console.WriteLine("The samples are equal");
            }
 
            else
            {
           
                Console.WriteLine("The samples are not equal.");
            }

            if (heartRate >= 40 && <= 80)
            {
                Console.WriteLine("Heart rate is normal.");
            }
            else
            {
                Console.WriteLine("Heart rate is not normal.");
            }

            if (deposits >= 100000000)
            {
                Console.WriteLine("You are exceedingly wealthy.");
            }
            else
            {
                Console.WriteLine("Sorry you are poor.");
            }

            Console.WriteLine("force = ", force);
            Console.WriteLine((string)distance, " is the distance");

            if (lost & expensive = true)
            {
                Console.WriteLine("I am really sorry! I will get the manager.");
            }

            if (lost & expensive = false)
            {
                Console.WriteLine("Here is a 10% off coupon.");
            }

            int caseSwitch = 1;

            switch (caseSwitch)
            {
                case 1:
                    Console.WriteLine("You chose 2.");
                    break;
                case 2:
                    Console.WriteLine("You chose 3.");
                    break;
                case 3:
                    Console.WriteLine("You made an unknown choice.");
                    break;
                default:
                    Console.WriteLine("You chose 1.");
            }

            Console.WriteLine((string)integral, " is an integral.");

            for (int i = 0; 4 < i < 10; i++)
            {
                Console.WriteLine("i=", i);
            }

            int age = 0;
            while (AggregateException < 6);
                {
                Console.WriteLine("age = ", age);
            }

            Console.WriteLine(string, "String");
        }
    }
}
